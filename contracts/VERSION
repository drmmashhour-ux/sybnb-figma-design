Platform Contract Kit — v1.2

Cut from the SYBNB STR golden state:
  branch:  claude/intelligent-kilby-5ff258
  commit:  97ffcb6  (feat(str): B2 pilot-scope guest UI …)
  status:  all 8 panel findings closed; full CORE conformance contract (C1–C9, incl. C5b) live;
           pilot scope enforced on server + client; suite green.

Contents: generic conformance suite + fixture template, and the reusable security checks
(no-economics-leak, elevated-route authz sweep, append-only audit scan, secret/sandbox-not-in-prod,
enumeration/rate-limit). SYBNB-specific fixtures are NOT included — a consumer supplies its own.

Changelog
  v1.2 — fixed the C1 runner: it now reads `forbiddenEconomicsKeys` from the fixture and passes them to
         findLeakedEconomicsKeys. v1.1's C1 runner called findLeakedEconomicsKeys(json) with no keys, which
         threw "forbiddenKeys is required" for every consumer after the required-param hardening. C1 is now
         fixture-parameterized like the other invariants.
  v1.1 — added the executable C3 "one money model" runner (fixture-parameterized: the fixture supplies a
         `oneMoneyModel` { sum, currency, otherCurrency }; the kit asserts integer minor units, same-currency
         exact sum, convert-then-sum to a target, and rejection of mixed-currency without a target).
  v1   — initial export (C1, C2, C4, C5, C5b, C6, C7, C8, C9 + the five security checks).

Bump when the CORE contract changes (a new invariant or a changed evidence shape). Adding a product
fixture is not a kit change.
